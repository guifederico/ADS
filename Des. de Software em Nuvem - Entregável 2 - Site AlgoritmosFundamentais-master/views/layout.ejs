<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&family=Permanent+Marker&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="site.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="descrição:" content="site de algoritmos fundamentais">
    <title><%= titulo %></title>
</head>

<body>
    <div class="topo">
        <p class="data">
            Você atualizou essa página: <%= new Date(); %>
        </p>
        <h1>
            <%= titulo %>
        </h1>
        <p class="autoria">Por Luiza Pires | Kevin Guedes | Sofia Oliveira | Camila Alves | Guilherme Federico</p>
        <div class="cabecalho">
            <div class="einstein">
                <img class="einsteinImg" src="img/einstein.png" alt="desenho de Einstein">
            </div>
            <div class="comentarios">
                <p class="comentario">Escolha a função que deseja utilizar</p>
            </div>

            <% botoes.map(botao => { %>
                <div class="<%= botao.class %>">
                    <div class="<%= botao.divClass %>">
                        <a href="<%= botao.href %>">
                            <p><%= botao.texto %></p>
                        </a>
                    </div>
                </div>                
            <% }) %>
        </div>
    </div>
    <div class="borda"></div>

    <%- body %>

    <div class="footer">
        <div class="bloco-footer">
            <h2>Quem somos:</h2>
            <% equipe.map(membro => { %>
                <div class="pessoal">
                    <a href="<%= membro.site %>">
                        <img class="foto-pessoa" src="<%= membro.src %>" alt="<%= membro.nome %>">
                    </a>
                    <p id="nome"><%= membro.nome %></p>
                </div>
            <% }) %>
        </div>
    </div>

</body>

</html>